---
title: Troubleshooting the HBA Broadcom 9400-16i & SAS Upgrades
date: 2026-02-02T13:31:00Z
summary: Identified the Broadcom 9400-16i as a likely clone or broken card after it failed to show up in BIOS. Ordered a 9305-24i from "The Art of the Server" as a replacement.
tags: [hardware, hba, troubleshooting, ebay, server-upgrades]
authors: ["@rodydavis", "@Tyler-Alcorn"]
---

# Troubleshooting the HBA Broadcom 9400-16i & SAS Upgrades

On this day I was able to troubleshoot the 9400-16i and found out that I have a broken or clone card which was white I got green flashing lights after post, but nothing i could do would get it to show up in BIOS (even EFI shell).

I ordered a 9305-24i from "The Art of the Server" on eBay and should be a much better card.

Turns out there are a lot of clones on the market coming from China and there is a huge price gap under $200 and over $500. My 9400-16i was $135 which I though was the correct price for used.

## DevLog Improvements

Today I've transformed the DevLog from a collection of Markdown files into full-blown microsite generated with Dart.

### Microsite Architecture
- **Static Site Generator**: Built a custom SSG in Dart (`scripts/build_site.dart`) that parses markdown and frontmatter.
- **Templating**: Switched from manual string replacement to [Mustache](https://pub.dev/packages/mustache_template) for better structure.
- **Improved UI**:
    - **Sidebar**: High-speed navigation to recent logs.
    - **Pagination**: Chronological browsing for log entries.
    - **Premium Aesthetics**: Dark mode, "Outfit" typography, and responsive grid layout.

### Developer Experience
- **Live Rebuilds**: Implemented a `scripts/dev_server.dart` using the `watcher` package to automatically rebuild the site on any change.
- **Link Verification**: Created a `scripts/check_links.dart` tool to ensure all internal links and assets are valid before deployment.
- **CLI Power**: Added `ArgParser` to the build script for easy configuration of base paths and GitHub usernames.

### Deployment & CI/CD
- **GitHub Actions**: Configured a workflow (`.github/workflows/deploy.yml`) to automatically build and deploy to GitHub Pages on every push to `main`.
- **Concurrency Support**: Added `concurrency` rules to prevent race conditions during rapid deployments.
- **RSS Feed**: Added an automated RSS feed generator (`rss.xml`) so you can follow the log in your favorite reader.


## CasaOS SSH Connection Failed
*by @Tyler-Alcorn*


Encountered an error while trying to connect to CasaOS via SSH. The connection was failing with a handshake error indicating no supported authentication methods remain.


```json
{
  "error": "ssh: handshake failed: ssh: unable to authenticate, attempted methods [none password], no supported methods remain",
  "func": "v1.PostSshLogin",
  "file": "/home/runner/work/CasaOS/CasaOS/route/v1/ssh.go", 
  "line": 41
}
```

### 1. Host Key Verification Failed
After attempting to SSH directly (`ssh root@192.168.0.150`), we hit a `WARNING: REMOTE HOST IDENTIFICATION HAS CHANGED!` error. This was likely due to a re-installation of the OS on the same IP.

**Fix:** Removed the old key from known_hosts:
```bash
ssh-keygen -R 192.168.0.150
```

### 2. Permission Denied (Forgot Password)
Once the host key was accepted, we couldn't log in due to a lost or unset root password (`Permission denied`).

**Fix:**
1.  Opened the **Proxmox Console** for the VM.
2.  Verified the user existed (`grep -E 'sh$' /etc/passwd`).
3.  Reset the root password:
    ```bash
    passwd root
    ```
4.  Ensured root login was allowed in `/etc/ssh/sshd_config` (`PermitRootLogin yes`) and restarted SSH.

Successful connection established!

## Mounting UNAS via Proxmox Bind Mount (LXC)
*by @Tyler-Alcorn*

We attempted to mount the UNAS NFS share directly inside the CasaOS LXC container, but hit repeated `Operation not permitted` errors. This is a common security restriction in unprivileged containers, even with the "NFS" feature flag enabled.

**The Solution: Host-Passthrough (Bind Mount)**
Instead of mounting *inside* the container, we mounted it on the Proxmox Host and passed the folder through.

### 1. Mount on Proxmox Host
In the **Proxmox Node Shell**:
```bash
# Create mount point
mkdir -p /mnt/pve/unas

# Mount NFS share
mount -t nfs 192.168.0.23:/var/nfs/shared/server /mnt/pve/unas
```

### 2. Pass to Container (Bind Mount)
Still in the Host Shell, we used `pct set` to map the host directory to the container directory:

```bash
# Syntax: pct set <VMID> -mp0 /host/path,mp=/container/path
pct set 150 -mp0 /mnt/pve/unas,mp=/mnt/unas
```

### 3. Result
The files immediately appeared in CasaOS at `/mnt/unas` without needing to fiddle with container privileges or AppArmor profiles.

