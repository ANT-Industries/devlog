name: devlog_builder
description: A simple script to build the devlog microsite.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  markdown: ^7.2.2
  yaml: ^3.1.2
